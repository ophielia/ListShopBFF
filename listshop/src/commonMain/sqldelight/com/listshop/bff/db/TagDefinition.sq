import kotlin.Boolean;

CREATE TABLE TagEntity (
    externalId TEXT,
    isGroup INTEGER AS Boolean NOT NULL,
    name TEXT,
    parentId TEXT,
    power TEXT,
    tagType TEXT,
    userId TEXT

);

insertIntoTagLookup:
INSERT INTO TagEntity(externalId, isGroup, name, parentId, power, tagType, userId)
VALUES (?,?,?,?,?,?,?);


removeAllTagLookups:
DELETE FROM TagEntity;

selectAllTagLookups:
SELECT TagEntity.*
FROM TagEntity;
